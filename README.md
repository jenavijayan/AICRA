
  # AI Education Membership Website

  This is a code bundle for AI Education Membership Website. The original project is available at https://www.figma.com/design/2dVjutY4jYMDoE1KuulMMR/AI-Education-Membership-Website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  echo "# AICRA" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/jenavijayan/AICRA.git
git push -u origin main